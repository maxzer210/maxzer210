# 🦊 Telegram-бот «Чайная Кицунэ»

Готовый Telegram-бот для чайного пространства с:

- системой лояльности гостей;
- личным чайным блокнотом (что пили, какой вкус, впечатления);
- персональным QR-кодом для идентификации гостя в акциях;
- админ-функцией фиксации визитов по QR-коду;
- блоком акций и понятным интерфейсом кнопок.

## Функции для гостей

- `🫖 Мой профиль` — имя, количество посещений, баллы, уровень.
- `🎁 Лояльность` — текущий уровень и сколько осталось до следующего.
- `📓 Чайный блокнот` — просмотр последних записей.
- `➕ Добавить заметку` — пошаговое добавление записи о чае.
- `🔳 Мой QR` — персональный QR-код гостя.
- `📣 Акции` — текущие предложения чайной.

## Функции для администратора

- `/visit <КОД>` — засчитать визит гостя по коду из QR.
- `✅ Отметить визит (админ)` — интерактивный ввод кода из сканера.

Админы задаются через переменную окружения `ADMIN_IDS`.

## Технически

- Python + `aiogram` (FSM-сценарии)
- SQLite (пользователи, визиты, заметки)
- Генерация QR (`qrcode` + Pillow)

## Быстрый запуск

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# заполните BOT_TOKEN и ADMIN_IDS
python bot.py
```

### Быстрый запуск в Windows (PowerShell)

Если вы работаете в `PS C:\...>`, используйте команды ниже:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
copy .env.example .env
# заполните BOT_TOKEN и ADMIN_IDS
python bot.py
```

Если вы получили проект архивом и в текущей папке нет `README.md`, `bot.py`, `requirements.txt`,
сначала откройте в VS Code именно папку проекта: `File -> Open Folder...`.

## Переменные окружения

Создайте `.env`:

```env
BOT_TOKEN=ваш_токен_бота
ADMIN_IDS=123456789,987654321
DB_PATH=tea_kitsune.db
```

## Тесты

```bash
pytest
```
